- Write Unit test for force dimord function cases:
    - freq_trl_posx_posy_time > trl_chan_time
    - Even more leading dimensions (before posx or chan)
    
- Fix animation function to be speedier (plotting.animate_grid_data)
- Fullfield snr ramping onsets and offsets for radial and target waves
- Rename rotating waves to spiral waves
- Would be nice if rotating/spiral waves would have a center that can be set
- Make Nice comments on the entire example
- Go through plotting functions, delete and include as necessary

